{% extends 'base.html.twig' %}

{% block title %}Vente{% endblock %}

{% block body %}
    <h1>Vente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idvente</th>
                <td>{{ vente.idvente }}</td>
            </tr>
            <tr>
                <th>Date de vente</th>
                <td>{{ vente.datevente ? vente.datevente|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Total Ttc</th>
                <td>{{ vente.totalTtc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vente_index') }}">Retour à la liste</a>

    <a href="{{ path('vente_edit', {'idvente': vente.idvente}) }}">Modifier</a>

    {{ include('vente/_delete_form.html.twig') }}
{% endblock %}
